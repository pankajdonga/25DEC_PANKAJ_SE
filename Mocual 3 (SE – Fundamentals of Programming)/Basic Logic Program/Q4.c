//Q.4 Find Area of Square formula : a = a2

#include<stdio.h>
#include<conio.h>
main()
{
      float a, area;
      printf("enter value of a:");
      scanf("%f", &a);
      area=a*a;
      printf("\nArea of square is = %f", area);
      getch();
           
}
